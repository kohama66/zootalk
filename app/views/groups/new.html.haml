.main
  .new-group
    .new-group__message
      どこかのだれかとランダムでグループを作ります
      %br/ 
      グループの名前を決めてください
    .new-group__form
      = form_with model: @group do |f|
        - @users.each do |user|
          %input{:name => "group[user_ids][]", :type => "hidden", :value => user.id}/
        %input{:name => "group[user_ids][]", :type => "hidden", :value => current_user.id}/
        = f.text_area :name
        = f.submit value: "作成", class: "btn"